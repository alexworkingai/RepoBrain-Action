tky:
  remote_enabled: true
  remote_allow_commands: ["ask", "explain"]
  remote_allow_branches: []      # пусто = разрешено в любой ветке локально
  remote_allow_repos: []         # пусто = разрешено в любом репо
  remote_fail_open: true
