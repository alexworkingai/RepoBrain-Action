name: RepoBrain Self-Service OIDC Envelope

on:
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write
  id-token: write

jobs:
  repobrain:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.comment.body, '/repobrain')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: alexworkingai/RepoBrain-Action@main
        with:
          profile: partner-pilot
          terms_accepted: "true"
          oidc_audience: repobrain-api
          # Enable this after Sprint 93C hosted verification/provisioning is available
          # or when you explicitly want Sprint 93B action-side OIDC gating now.
          # self_service_mode: "true"
